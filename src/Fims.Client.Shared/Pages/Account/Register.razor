@page "/account/register"

<section class="section-content padding-y bg">

    <div class="card mx-auto" style="max-width:520px; margin-top:40px;">
        <article class="card-body">
            <header class="mb-4"><h4 class="card-title">Register</h4></header>
            <EditForm Model="model" OnValidSubmit="SubmitAsync">

                <ErrorsList ShowErrors="ShowErrors" Errors="Errors" />
                <DataAnnotationsValidator />
                <ValidationSummary />

                <div class="form-row">
                    <label>사용자 ID</label>
                    <InputText @bind-Value="model.UserName" type="text" class="form-control" />
                </div>
                <div class="form-row">
                    <label>이름</label>
                    <InputText @bind-Value="model.HangulName" type="text" class="form-control" />
                </div>
                <div class="form-row">
                    <label>E-mail</label>
                    <InputText @bind-Value="model.Email" type="email" class="form-control" />
                </div>
                <div class="form-row">
                    <label>Password</label>
                    <InputText @bind-Value="model.Password" type="password" class="form-control" />
                </div>

                <div class="form-group">
                    <button type="submit" class="btn btn-primary btn-block"> Register </button>
                </div>
            </EditForm>
        </article>
    </div>

    <p class="text-center mt-4">Have an account? <a href="/account/login">Log In</a></p>
    <br /><br />

</section>
