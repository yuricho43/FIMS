@using Telerik.Blazor.Components.Common.Icon

@inject LocalStorageInterop LocalStorage
@inject IJSRuntime JsInterop

<TelerikGrid Data=@MyTSheetSpec.ObservableTItemSpecsInCategoryDict[TCategory]
             EditMode="@GridEditMode.Incell"
             @ref="@TItemSpecGrid"
             OnUpdate="@UpdateHandler" OnEdit="@EditHandler" OnDelete="@DeleteHandler" OnCreate="@CreateHandler"
             Height="592px"
             RowHeight="50"
             ScrollMode="@GridScrollMode.Virtual"
             PageSize="20"
             OnStateInit="@((GridStateEventArgs<TItemSpec> args) => OnStateInit(args))"
             @bind-Page="@CurrentPage">

@*    <GridToolBar>
        <GridSearchBox Width="150px"></GridSearchBox>
        <span class="k-toolbar-spacer"></span>
        <GridCommandButton Command="ExcelExport" Icon="file-excel">@Localizer["Grid_ExcelExport"]</GridCommandButton>
        <GridCommandButton Command="CsvExport" Icon="file-csv">@Localizer["Grid_CsvExport"]</GridCommandButton>
    </GridToolBar>
*@
    <GridColumns>
        <GridColumn Field="@nameof(TItemSpec.TestNo)" Width="50px" Title="TestNo" Editable="false" HeaderClass="center-me" TextAlign="@ColumnTextAlign.Center">
            <HeaderTemplate><strong>Test No</strong></HeaderTemplate>
            <Template>
                <span class="k-badge k-badge-lg k-badge-solid k-badge-solid-success k-rounded-lg" style="background-color:teal">@((context as TItemSpec).TestNo)</span>
            </Template>
        </GridColumn>

        <GridColumn Field="@nameof(TItemSpec.Title)" Width="150px" Title="항목" Editable="false" HeaderClass="center-me">
            <HeaderTemplate><strong>시험항목</strong></HeaderTemplate>
            <Template>
                <span>@((context as TItemSpec).Title)</span>
                <TelerikTooltip TargetSelector=".tooltip-target" />
                <span title="@((context as TItemSpec).RangeToolTip)" class="tooltip-target" style="float:right"><TelerikIcon Icon="@FontIcon.QuestionCircle" /></span>
            </Template>
        </GridColumn>

        <GridColumn Field="@nameof(TItemSpec.Ch1Data)" Width="100px" OnCellRender="@((GridCellRenderEventArgs args) => OnCellRenderHandler(args, "Ch1Data"))" Title="CH1" HeaderClass="center-me" TextAlign="@ColumnTextAlign.Center">
            <HeaderTemplate><strong>CH1</strong></HeaderTemplate>
            <Template>
                @{
                    var tItemSpec = context as TItemSpec;
                    if (tItemSpec.IsCh1DataEnabled)
                    {
                        if (tItemSpec.ExpressionMode == "String/Input(Time)")
                        {
                            <span>@tItemSpec.Ch1Data</span>
                            <span>&nbsp;</span>
                            <span class="auto-fill">@tItemSpec.Ch1Time?.ToString("HH:mm:ss")</span>
                        }
                        else
                        {
                            <span>@tItemSpec.Ch1Data</span>
                        }
                    }
                    else
                    {
                        <div tabindex="0" style="display:none"></div>
                    }
                }
            </Template>
            <EditorTemplate>
                @{
                    var tItemSpec = context as TItemSpec;
                    if (tItemSpec.IsCh1DataEnabled)
                    {
                        if (tItemSpec.ExpressionMode == "String/Combo")
                        {
                            <TelerikDropDownList Data="@tItemSpec.UnitList" @bind-Value="@tItemSpec.Ch1Data" Class="k-focused-cell" />
                        }
                        else
                        {
                            <TelerikTextBox @bind-Value=tItemSpec.Ch1Data Class="k-grid k-focused-cell"></TelerikTextBox>
                        }
                    }
                    else
                    {
                        <div tabindex="0" style="display:none"></div>
                    }
                }
            </EditorTemplate>
        </GridColumn>

        <GridColumn Field="@nameof(TItemSpec.Ch2Data)" Width="100px" OnCellRender="@((GridCellRenderEventArgs args) => OnCellRenderHandler(args, "Ch2Data"))" Title="CH2" HeaderClass="center-me" TextAlign="@ColumnTextAlign.Center">
            <HeaderTemplate><strong>CH2</strong></HeaderTemplate>
            <Template>
                @{
                    var tItemSpec = context as TItemSpec;
                    if (tItemSpec.IsCh2DataEnabled)
                    {
                        if (tItemSpec.ExpressionMode == "String/Input(Time)")
                        {
                            <span>@tItemSpec.Ch2Data</span>
                            <span>&nbsp;</span>
                            <span class="auto-fill">@tItemSpec.Ch2Time?.ToString("HH:mm:ss")</span>
                        }
                        else
                        {
                            <span>@tItemSpec.Ch2Data</span>
                        }
                    }
                    else
                    {
                        <div tabindex="0" style="display:none"></div>
                    }
                }
            </Template>
            <EditorTemplate>
                @{
                    var tItemSpec = context as TItemSpec;
                    if (tItemSpec.IsCh2DataEnabled)
                    {
                        if (tItemSpec.ExpressionMode == "String/Combo")
                        {
                            <TelerikDropDownList Data="@tItemSpec.UnitList" @bind-Value="@tItemSpec.Ch2Data" Class="k-focused-cell" />
                        }
                        else
                        {
                            <TelerikTextBox @bind-Value=tItemSpec.Ch2Data Class="k-grid k-focused-cell"></TelerikTextBox>
                        }
                    }
                    else
                    {
                        <div tabindex="0" style="display:none"></div>
                    }
                }
            </EditorTemplate>
        </GridColumn>
 
        <GridColumn Field="@nameof(TItemSpec.Ch3Data)" Width="100px" OnCellRender="@((GridCellRenderEventArgs args) => OnCellRenderHandler(args, "Ch3Data"))" Title="CH3" HeaderClass="center-me" TextAlign="@ColumnTextAlign.Center">
            <HeaderTemplate><strong>CH3</strong></HeaderTemplate>
            <Template>
                @{
                    var tItemSpec = context as TItemSpec;
                    if (tItemSpec.IsCh3DataEnabled)
                    {
                        if (tItemSpec.ExpressionMode == "String/Input(Time)")
                        {
                            <span>@tItemSpec.Ch3Data</span>
                            <span>&nbsp;</span>
                            <span class="auto-fill">@tItemSpec.Ch3Time?.ToString("HH:mm:ss")</span>
                        }
                        else
                        {
                            <span>@tItemSpec.Ch3Data</span>
                        }
                    }
                    else
                    {
                        <div tabindex="0" style="display:none"></div>
                    }
                }
            </Template>
            <EditorTemplate>
                @{
                    var tItemSpec = context as TItemSpec;
                    if (tItemSpec.IsCh3DataEnabled)
                    {
                        if (tItemSpec.ExpressionMode == "String/Combo")
                        {
                            <TelerikDropDownList Data="@tItemSpec.UnitList" @bind-Value="@tItemSpec.Ch3Data" Class="k-focused-cell" />
                        }
                        else
                        {
                            <TelerikTextBox @bind-Value=tItemSpec.Ch3Data Class="k-grid k-focused-cell"></TelerikTextBox>
                        }
                    }
                    else
                    {
                        <div tabindex="0" style="display:none"></div>
                    }
                }
            </EditorTemplate>
        </GridColumn>

        <GridColumn Field="@nameof(TItemSpec.Ch4Data)" Width="100px" OnCellRender="@((GridCellRenderEventArgs args) => OnCellRenderHandler(args, "Ch4Data"))" Title="CH4" HeaderClass="center-me" TextAlign="@ColumnTextAlign.Center">
            <HeaderTemplate><strong>CH4</strong></HeaderTemplate>
            <Template>
                @{
                    var tItemSpec = context as TItemSpec;
                    if (tItemSpec.IsCh4DataEnabled)
                    {
                        if (tItemSpec.ExpressionMode == "String/Input(Time)")
                        {
                            <span>@tItemSpec.Ch4Data</span>
                            <span>&nbsp;</span>
                            <span class="auto-fill">@tItemSpec.Ch4Time?.ToString("HH:mm:ss")</span>
                        }
                        else
                        {
                            <span>@tItemSpec.Ch4Data</span>
                        }
                    }
                    else
                    {
                        <div tabindex="0" style="display:none"></div>
                    }
                }
            </Template>
            <EditorTemplate>
                @{
                    var tItemSpec = context as TItemSpec;
                    if (tItemSpec.IsCh4DataEnabled)
                    {
                        if (tItemSpec.ExpressionMode == "String/Combo")
                        {
                            <TelerikDropDownList Data="@tItemSpec.UnitList" @bind-Value="@tItemSpec.Ch4Data" Class="k-focused-cell" />
                        }
                        else
                        {
                            <TelerikTextBox @bind-Value=tItemSpec.Ch4Data Class="k-grid k-focused-cell"></TelerikTextBox>
                        }
                    }
                    else
                    {
                        <div tabindex="0" style="display:none"></div>
                    }
                }
            </EditorTemplate>
        </GridColumn>
    </GridColumns>
</TelerikGrid>


<style>
    .k-grid td, .k-grid tr {
        background-color: #CACACA;
        color: black;
        border-width: 0px 1px 1px 0px !important;
        border-color: #ABB2B9 !important;
    }

    .k-grid-header-wrap th {
        background-color: teal;
        color: white;
        border-width: 0px 1px 1px 0px !important;
        border-color: #ABB2B9 !important;
    }
 
/*    .k-grid .k-focused-cell {
        background-color: cornflowerblue;
    }
*/
    .k-grid .k-disabled-cell {
        background-color: #CACACA;
    }

    .k-grid .userinput-empty {
        background-color: #e6f3ff;
    }

    .k-grid .userinput-valid {
        color: dodgerblue;
        background-color: #b3daff;
        font-weight: bold;
    }

    .k-grid .userinput-invalid {
        color: red !important;
        background-color: #ffeecc;
        font-weight: bold !important;
    }

    /* CENTER */
    /* non-sortable headers */
    .k-grid th.center-me {
        text-align: center;
    }

    /* sortable headers */
    th.center-me .k-cell-inner > .k-link {
        justify-content: center;
    }

    /* RIGHT */
    /* non-sortable headers */
    .k-grid th.align-right {
        text-align: right;
    }

    /* sortable headers */
    th.align-right .k-cell-inner > .k-link {
        justify-content: right;
    }

    /* unbound and non-filterable columns */
    .k-grid-header th.align-right.right-padding {
        padding-right: 12px;
    }

    /* filterable grid, sortable non-filterable columns */
    .k-grid-header th.align-right.right-padding > .k-cell-inner {
        margin-right: 0;
    }
</style>


<style>
    .auto-fill {
        font-size: 0.8em;
        font-weight: normal;
        color: black;
    }
</style>

